{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "mkdir -p bootstrap/cache && chmod -R 775 bootstrap/cache && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}