{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "composer install && mkdir -p bootstrap/cache && chmod -R 775 bootstrap/cache && php artisan key:generate && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}